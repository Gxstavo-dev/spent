// manejo de formularios
// Importamos los formularios desde elementos.ts
import { formLogin, formRegistro } from "./elementos";

// Cuando se envie el formulario de login (por ahora vacio)
formLogin.addEventListener("submit", () => {});

// Cuando se envie el formulario de registro (por ahora vacio)
formRegistro.addEventListener("submit", () => {});
